# G-52-3d0 March Report: Backlog Cleanup

**Goal:** Backlog cleanup -- restore expansion, QA concern resolution, stale tool refs, auto-detect test counts, evolutionary backlog pruning
**Date:** 2026-03-23
**Outcome:** Complete. Single phase, 3 tasks, 2 waves. All succeeded first try. 896 tests, 0 errors, 0 warnings.

## 1. Summary

G-52 executed five backlog items accumulated during G-51's evolutionary runs. A sixth item (concern keyword extraction) was discovered already implemented and dropped during research. The goal compressed six pre-investigated backlog items into three tasks across two files, demonstrating the leanest goal type in the pipeline: zero SOS, zero replanning, and compressed seed overhead (researcher + planner only, no auditor needed for pre-investigated items).

## 2. Changes

### restore() expansion (MemoryTools.cs)
- `memory('restore')` now enumerates and inlines **all** `agent:*` memories (profile, tenets, execution-policy) under an "Agent norms" section header, replacing the previous hardcoded single `agent:profile` load.
- Also enumerates and inlines **all** `patterns:*` memories (7 entries: apikey-security, background-agents, bundle-validation, file-conflict-analysis, httpcontent-reuse, resilience, resource-lifecycle) under a "Learned patterns" section header.
- Both enumerations wrapped in try/catch for missing scopes and individual entry read failures.

### QA skill update (ProjectTools.cs)
- Added step 6 ("Resolve addressed concerns") to the built-in QA skill methodology.
- QA agents now call `concern('resolve')` for each concern their verification evidence confirms is fixed.

### Evolutionary skill updates (ProjectTools.cs)
- New step 5 ("Verify test counts"): instructs the evolutionary agent to run the project's test command and update count-bearing memories when stored counts drift from actual.
- Step 6 ("Scan backlog for unblocked and resolved items") expanded with three sub-bullets: Unblocked (flag for promotion), Resolved (remove completed entries), Stale (flag items with 3+ goals of no progress).
- Steps renumbered 1-8 with no gaps.

### Stale tool reference cleanup (ProjectTools.cs, MemoryTools.cs)
- 40 occurrences of old underscore-style tool syntax (`task_next`, `plan_verify`, `concern_resolve`, etc.) replaced with `noun('action')` syntax across user-facing strings, internal state values, and code comments.

### Files touched
| File | Insertions | Deletions |
|------|-----------|-----------|
| `src/Scrinia.Mcp/MemoryTools.cs` | +40 | -2 |
| `src/Scrinia.Mcp/ProjectTools.cs` | +52 | -43 |
| **Total** | **+92** | **-45** |

## 3. Findings

| ID | Description | Severity | Status | Resolution |
|----|-------------|----------|--------|------------|
| QAL-042 | Removing "just do it" clause may cause goal friction for trivial fixes | Low | Active | Accepted risk -- replacement text still allows direct action for single-line changes |
| QAL-043 | CalculateProgress called at 7+ sites adds N index loads per state write | Low | Active | Accepted risk -- in-memory dictionary lookups, negligible performance impact |
| QAL-045 | CalculateProgress promoted to internal static increases API surface | Low | Active | Accepted risk -- XML doc comment documents its contract |
| QAL-065 | Backlog item "concern keyword poverty" is stale -- already implemented | Low | Active | Raised during G-52 research; validates the evolutionary skill's new "Resolved" sub-bullet |

All four active concerns are low severity and carry no immediate risk. QAL-042, QAL-043, and QAL-045 are inherited from G-49/G-50. QAL-065 was raised during G-52 research when item 5 was discovered already implemented.

## 4. Test Impact

- **Before:** 896 tests (821 Scrinia.Tests + 63 Server.Tests + 12 Embeddings.Tests)
- **After:** 896 tests (821 + 63 + 12), 0 failed, 0 skipped
- **New tests added:** 0

The existing test at `ProjectLifecycleTests.cs` line 298 was updated to assert the new "Agent norms" section header from the restore() expansion. No net-new test methods were added. The retrospective notes this as a continuation of the zero-test-growth pattern from G-51, though for string-literal and enumeration changes the risk profile is low.

## 5. Security Posture

No security changes in this goal. G-52 was a pure backlog cleanup targeting skill methodology text and restore() context loading.

**Remaining known issues:** None security-related. The four active concerns (QAL-042/043/045/065) are all code quality, not security.

## 6. Configuration Changes

No configuration changes. No new settings, no breaking changes, no migration needed.

---

*Generated by march-reporter skill, 2026-03-23. Sources: research:g52-3d0-backlog-cleanup, research:g52-3d0-summary, qa:latest (G-52-3d0 Phase 01), learn:retro-g52-3d0-01, quality:applied-fixes, concern:QAL-042/043/045/065.*
