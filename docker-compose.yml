services:
  scrinium:
    build:
      context: .
      dockerfile: src/Scrinia.Server/Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - scrinium-data:/data
    environment:
      - Scrinia__DataDir=/data
    restart: unless-stopped

volumes:
  scrinium-data:
